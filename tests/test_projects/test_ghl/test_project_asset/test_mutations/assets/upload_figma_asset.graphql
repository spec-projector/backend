mutation ($input: CreateProjectAssetInput!) {
  uploadFigmaAsset(input: $input) {
    projectAsset {
      project {
        id
      }
      file {
        id
        url
      }
      source
    }
  }
}
